# acl-proxy documentation

All user-facing documentation lives in the [project README](../README.md).

This directory contains internal design and implementation notes:

- `design/` — Architecture and design documents for specific features.
- `implementation/` — Detailed implementation notes for recent feature work. These notes may reference historical file paths (e.g., `docs/config-reference.md`) that existed at the time of implementation — all user-facing content now lives in the project README.
